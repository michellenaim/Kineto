<!DOCTYPE html>
<html>
  <head>
    <title>Kineto</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="" type="text/css">
    <script src="https://kit.fontawesome.com/7bdd1d32e3.js" crossorigin="anonymous"></script>
    <script type="text/javascript">
        window.kinetoLogo = "<%= image_url('Kineto_logo.png') %>";
     </script>

    <meta name="description" content="Kineto App" />
    <meta name="title" property="og:title" content="Kineto" />
    <meta property="og:type" content="Website" />
    <meta name="image" property="og:image" content="<%= image_url('Kineto_logo.png') %>" />
    <meta name="description" property="og:description" content="A Netflix clone that allows users to watch movie trailers on-demand built with Ruby on Rails and React/Redux" />
    <meta name="author" content="Michelle Naim" />
     

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= favicon_link_tag asset_path('favicon.ico') %>
    <% if current_user %>
    <script>
      window.currentUser = {
        "id": <%= current_user.id %>, 
        "email": "<%= current_user.email %>",
      }
    </script>
    <% end%>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
